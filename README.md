# 60035 Natural Language Processing Coursework Code

## Setup
<span style="font-family: monospace">./baseline_models</span> : contains saved model states for the baseline model <br>
<span style="font-family: monospace">./baseline_models</span> : contains saved model states for the baseline model <br>
<span style="font-family: monospace">./baseline_models</span> : contains saved model states for the baseline model <br>
<span style="font-family: monospace">./baseline_models</span> : contains saved model states for the baseline model <br>
<span style="font-family: monospace">./baseline_models</span> : contains saved model states for the baseline model <br>
